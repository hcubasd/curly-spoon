name: integration
on:
  push:
    branches:
      - "**"
jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: docker compose up --exit-code-from migrate
